export interface CompletionPropertyInfo {
    name: string;
    friend?: string;
    friendNoSkip?: boolean;
    desc?: string;
    snippet?: string;
}

const properties: CompletionPropertyInfo[] = [
    {
        name: '',
    }
];